const isAdmin = true;
const myNumber = 100;
const user = "Marco";
let welcome;
if (isAdmin) {
    welcome = `Hello ${user}`;
}
else {
    welcome = `${user} is not an Admin,`;
}
console.log(welcome);
console.log(welcome, "hihihi");
